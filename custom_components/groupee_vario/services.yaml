refresh_now:
  name: Refresh now
  description: Force an immediate refresh of tariff data from the Groupe E API.
  fields:
    entry_id:
      name: Config entry ID
      description: Optional. If set, refresh only this config entry. If omitted, refresh all Groupe E Tariffs entries.
      example: "0123456789abcdef0123456789abcdef"
      selector:
        text:
